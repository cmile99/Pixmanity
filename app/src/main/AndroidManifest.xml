<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.threeklines.pixmanity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".containers.ChatSupportContainer"></activity>
        <activity android:name=".containers.BookAppointment2Container" />
        <activity android:name=".containers.BookAppointmentContainer" />
        <activity android:name=".containers.CompletedProjectsContainer" />
        <activity android:name=".containers.ProjectsDashContainer" />
        <activity android:name=".containers.MyAppointmentsContainer" />
        <activity android:name=".containers.MyProfileContainer" />
        <activity android:name=".containers.ControlCenterActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>